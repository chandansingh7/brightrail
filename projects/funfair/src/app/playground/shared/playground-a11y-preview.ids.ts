import { PLAYGROUND_A11Y_PREVIEW_LOADERS } from './playground-a11y-preview.registry';

/** Sorted component ids registered for the isolated a11y preview route. */
export const PLAYGROUND_A11Y_PREVIEW_IDS = Object.keys(PLAYGROUND_A11Y_PREVIEW_LOADERS).sort();
